const STRING = 0;
const NUMBER = 1;
const BOOLEAN = 2;
const RANGE = 3;
const ENUM = 4;
const OBJECT = 5;
const ARRAY = 6;
const DATE = 7;

const dataTypes = {
    STRING,
    NUMBER,
    BOOLEAN,
    RANGE,
    ENUM,
    OBJECT,
    ARRAY,
    DATE
};

module.exports = dataTypes;
