<div>
    <canvas id="classes_chart"></canvas>
    <script src="/javascripts/reports/charts/classes.js"></script>
    <script>
        const classes_labels = <%- JSON.stringify(labels) %>;
        const classes_data = <%- JSON.stringify(datasets[0]) %>;
        const classes_backgrounds = <%- JSON.stringify(deviceCategory.getList().map(category => deviceCategory.deviceCategoryToRGB(category))) %>;
        const classes_borderColours = <%- JSON.stringify(deviceCategory.getList().map(category => deviceCategory.deviceCategoryToRGB(category, true))) %>;
    </script>
</div>